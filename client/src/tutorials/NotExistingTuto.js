import React from 'react'
import { Typography } from '@material-ui/core'
export default class NotExistingTuto extends React.Component {
    render() {
        return (
            <Typography>Cette rubrique n'existe pas</Typography>
        )
    }
}